


4/30/26:

Working well. To pick back up, let's:

0. Let's just make a quick route to check if the server is "on" or not. Running the client-side without the server running 
    shouldn't return with "Could not load links: Server returned non-JSON response (HTTP 500)" or whatever specific route 
    we're running. We should just have an indicator light at the top that has:
        - red: could not connect to port [port] ("could not connect to port [port]")
        - yellow: connected to server with issues ("issue named here");
        - green: server connected, connected to port [port]

1. Play with the outputs. Already, I can see we're getting leading + trailing text in cells with "description: https//link.com" 
    - Probably the right logic here is to:
        a. read until 'h' or '<' char detected
        b. go from there until end
        c. if no 'h' nor '<' char detected, just put the whole cell in and "console.error" the contents of the cell 

2. DB isn't tested at all. So clearly, we will need to setup test cases and diagnose any:
    - functional issues
    - security vulnerabilities





According to Claude, "What do you think we should work on?":

Immediate / most impactful
Delete src/lib/parseSheet.js — it's still sitting there orphaned, we just never cleaned it up

Test the full flow end-to-end with a real VTS file and make sure links are actually being stored and displayed correctly
Functional gaps
Domains and Macros management UI — right now those tables exist in the DB but there's no way to add/edit them from the app. Without domains seeded, every link shows null for domain. Without macros seeded, no macro matching happens. These tabs are probably the next big feature.
Macro regex logic — we're currently using url.includes(macro.macro_pre) which is a simple string match. Depending on what VTS macro tokens actually look like, we may need a proper regex pattern here.
Polish / reliability
last_modified auto-update on edits — the UPDATE queries in domains.js and macros.js set last_modified = now() manually, but it'd be cleaner as a Postgres trigger so it's enforced at the DB level
Session handling edge case — if the session stored in sessionStorage is stale (e.g. DB was wiped), the app will silently fail on link extraction. We should detect and recover from that.
I'd probably go 2 → 3 in that order. What sounds right to you?